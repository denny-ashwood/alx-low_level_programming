#include "main.h"
#include <stdlib.h>

/**
 * _abs - computes the absolute value of an integer
 *
 * @x: the integer passed to write absolute value
 *
 * Return: Always 0.
 */

int _abs(int x)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
